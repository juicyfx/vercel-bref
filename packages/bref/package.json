{
  "name": "now-bref",
  "description": "Deploy bref to ZEIT Now",
  "version": "0.0.1-canary.10",
  "license": "MIT",
  "main": "./dist/index.js",
  "homepage": "https://github.com/juicyfx/now-bref",
  "repository": {
    "type": "git",
    "url": "https://github.com/juicyfx/now-bref.git"
  },
  "scripts": {
    "watch": "tsc --watch",
    "build": "tsc",
    "test": "tsc && jest --verbose",
    "test-watch": "jest --verbose --watch",
    "prepublishOnly": "tsc"
  },
  "files": [
    "dist",
    "lib"
  ],
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/spec/**/*.[jt]s?(x)"
    ],
    "rootDir": "."
  },
  "dependencies": {
    "@now-bref/lib": "experimental"
  },
  "devDependencies": {
    "@now/build-utils": "^0.9.4",
    "@types/glob": "^7.1.1",
    "@types/node": "^10.0.0",
    "jest": "^24.8.0",
    "typescript": "^3.5.3"
  }
}
